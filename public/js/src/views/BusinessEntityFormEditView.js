define(function(require) {
    'use strict';
    var Backbone = require('backbone');

    var BusinessEntityFormEditView = Backbone.View.extend({

        initialize: function(options) {
            
        },

        render: function() {
        }
    });
    return BusinessEntityFormEditView;
});